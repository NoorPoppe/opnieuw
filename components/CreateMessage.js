export default function CreateMessage({ to, from, lie }) {
    return (
        <div>

            <div>
                <div>To: {to}</div>
                <div>From: {from}</div>
                <div>Lie: {lie}</div>
            </div>
        </div>
    )
}
